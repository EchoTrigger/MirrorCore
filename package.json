{
  "name": "mirrorcore",
  "version": "0.1.0",
  "description": "智能模块化AI代理系统 - 通过智能判断自动加载功能模块，实现传统聊天无法完成的高级任务",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install --prefix shared && npm install --prefix backend && npm install --prefix desktop",
    "dev": "concurrently \"npm:dev:backend\" \"npm:dev:desktop\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:desktop": "npm run dev --prefix desktop",
    "build": "npm run build:all",
    "build:all": "npm run build --prefix backend && npm run build --prefix desktop",
    "build:backend": "npm run build --prefix backend",
    "build:desktop": "npm run build --prefix desktop",
    "build:prod": "npm run build:all",
    "start-all": "concurrently \"npm:dev:backend\" \"npm:dev:desktop\"",
    "test": "npm run test --prefix backend && npm run test --prefix desktop",
    "lint": "npm run lint --prefix backend && npm run lint --prefix desktop",
    "format": "npm run format --prefix backend && npm run format --prefix desktop",
    "format:check": "npm run format:check --prefix backend && npm run format:check --prefix desktop"
  },
  "keywords": [
    "ai",
    "assistant",
    "intelligent-agent",
    "modular-system",
    "electron",
    "nodejs",
    "typescript",
    "smart-modules",
    "extension-store",
    "intent-recognition"
  ],
  "author": "EchoTrigger",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/EchoTrigger/MirrorCore.git"
  },
  "homepage": "https://github.com/EchoTrigger/MirrorCore#readme",
  "bugs": {
    "url": "https://github.com/EchoTrigger/MirrorCore/issues"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}