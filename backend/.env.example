# AI 服务配置
# 支持的提供商: openai, claude, qwen
AI_PROVIDER=qwen

# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
# 支持的模型: gpt-3.5-turbo, gpt-4, gpt-4-turbo, gpt-4-vision-preview
# gpt-4-vision-preview: 支持图像理解的视觉语言模型
OPENAI_MODEL=gpt-3.5-turbo

# Claude 配置
CLAUDE_API_KEY=your_claude_api_key
# 支持的模型: claude-3-opus-20240229, claude-3-sonnet-20240229, claude-3-haiku-20240307
CLAUDE_MODEL=claude-3-sonnet-20240229

# 通义千问配置
# 通义千问API密钥 (与DASHSCOPE_API_KEY相同)
QWEN_API_KEY=your_qwen_api_key
# 达摩院灵积平台API密钥 (与QWEN_API_KEY相同)
DASHSCOPE_API_KEY=your_qwen_api_key
# 通义千问API基础URL
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# 支持的模型: qwen-turbo, qwen-plus, qwen-max, qwen-max-longcontext, qwen-vl-plus
# qwen-vl-plus: 支持图像理解的视觉语言模型
QWEN_MODEL=qwen-vl-plus

# 服务器配置
PORT=3000

# 流式响应配置
# 是否启用思考过程 (true/false)
ENABLE_THINKING=true
# 思考过程的最大token数
THINKING_BUDGET=1000

# 搜索配置（可在前端设置界面中调整并写入 .env）。以下为默认初始值，前端可热更新运行时配置覆盖。
# 说明：
# - SEARCH_METHOD 可选：DuckDuckGo | Playwright | Auto
# - SEARCH_ENGINE 可选：DuckDuckGo | Google | Bing | Baidu
#   注意：当 SEARCH_METHOD=Playwright 时，SEARCH_ENGINE 仅支持 Google/Bing/Baidu；
#         当 SEARCH_METHOD=DuckDuckGo 时，ENGINE 不用于 Playwright。
# - MAX_SEARCH_QUESTIONS：一次用户查询最多拆分多少子问题（预留，当前不强制使用）
# - MAX_SEARCH_RESULTS：每次搜索返回的最大结果条数（作为默认值）

SEARCH_METHOD=Playwright
SEARCH_ENGINE=Bing
MAX_SEARCH_QUESTIONS=3
MAX_SEARCH_RESULTS=12

# 另一种示例：使用 DuckDuckGo
# SEARCH_METHOD=DuckDuckGo
# SEARCH_ENGINE=DuckDuckGo
# MAX_SEARCH_QUESTIONS=3
# MAX_SEARCH_RESULTS=12

# 其他可选项（未在前端设置中暴露时，仍可通过 .env 调整）
# 搜索默认语言区域（Playwright 模式下可用）
# SEARCH_LOCALE=zh-CN
# 是否使用无头浏览器（Playwright）
# SEARCH_HEADLESS=true
# 搜索超时时间（毫秒）
# SEARCH_TIMEOUT_MS=10000

# 网络环境提示/回退设置
# 若您的网络无法访问 YouTube，可设置：
# BLOCK_YOUTUBE=true
# 这样在“打开 YouTube 并搜索 …”的意图解析中，会自动回退到 Bilibili。
